<div class="jumbotron">
  <p class="lead">{{resource.resource}}</p>
  <p>{{resource.mood}}</p>
  <p class="lead">
    <form action="/user/{{currentUser._id}}/favorites/{{resource._id}}" method="post">
      <div><button type='submit' class='btn btn-primary'>Save</button></div>
    </form>
    <button><a href="/angry">Refresh</a></button>
  </p>
</div>