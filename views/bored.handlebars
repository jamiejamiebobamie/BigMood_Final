<div class="jumbotron">
  <p class="lead">{{resource.resource}}</p>
  <p>{{resource.mood}}</p>
  <p class="lead">
    <button><a href="/bored">Refresh</a></button>
  </p>
</div>



<form action="/user/{{currentUser._id}}/favorites" method="post">
  <div class="text-right">
    <button type="submit" class="btn btn-primary">Save</button>
  </div>
</form>