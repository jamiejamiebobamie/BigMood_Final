<div class="jumbotron">
  <p class="lead">{{resource.resource}}</p>
  <p><a href="{{resource.author}}">{{resource.author}}</a></p>
  <p>{{resource.mood}}</p>
  <p class="lead">
      <button><a href="/angry">Refresh</a></button>
  </p>
</div>


<!-- <h3><center><a href="/bored">Refresh</a></center></h3> -->

<form action="/user/{{currentUser._id}}/resources/{{resource._id}}" method="post">
  <div class="text-right">
    <button type="submit" class="btn btn-primary">Save</button>
  </div>
</form>
