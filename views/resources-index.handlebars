<h1>Resources Index</h1>

<div class="row">
  {{#each resources}}
    <div class="col-sm-3">
      <h2><a href="/resources/{{this._id}}">{{this.resource}}</a></h2>
      <!-- NOTE: this.resource above might be this.content -->
      <small>{{this.author}}</small>
    </div>
  {{/each}}
</div>
