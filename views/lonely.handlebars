<div class="jumbotron">
  <p class="lead">{{resource.resource}}</p>
  <p>{{resource.mood}}</p>
  <p class="lead">
    <form action="/user/favorites/{{resource._id}}" method="post">
      <div><button type='submit' class='btn btn-primary'>Save</button></div>
    </form>
  </p>
</div>

<div>
  <button><a href="/lonely">Refresh</a></button>
</div>