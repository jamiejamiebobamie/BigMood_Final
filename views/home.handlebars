<div class="jumbotron">
  <p class="lead">{{resource.resource}}</p>
  <p><a href="{{resource.author}}">{{resource.author}}</a></p>
  <p>{{resource.hashtag}}</p>
  <p class="lead">
  </p>
  <form method="POST" action="/lists">
      <button  type="submit" class="btn btn-primary" >Save To List</button>
  </form>
</div>



<h3><center><a href="/">Refresh</a></center></h3>

<!-- /{{list._id}}?_method=PUT -->
